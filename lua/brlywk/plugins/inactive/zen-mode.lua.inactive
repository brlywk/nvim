return {
	{
		"folke/zen-mode.nvim",
		opts = {},
		config = function()
			vim.keymap.set("n", "<leader>wz", vim.cmd.ZenMode, { desc = "Zen Mode" })
		end,
	},

	-- make zen mode extra nice!
	{
		"folke/twilight.nvim",
		opts = {
			-- your configuration comes here
			-- or leave it empty to use the default settings
			-- refer to the configuration section below
		},
	},
}
